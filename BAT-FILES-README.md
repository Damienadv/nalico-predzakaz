# BAT файлы для Windows 🪟

## Доступные команды

### 1. `start.bat` - Быстрый запуск (рекомендуется)
**Что делает:**
- Проверяет установку Node.js
- Устанавливает зависимости (при первом запуске)
- Конвертирует последний Excel файл в JSON
- **Автоматически открывает дашборд в браузере**

**Использование:**
```
Двойной клик на start.bat
```

---

### 2. `update.bat` - Обновление данных
**Что делает:**
- Ищет последний по дате Excel файл в `price_files/`
- Конвертирует его в `data.json`
- Выводит статус операции

**Использование:**
```
Двойной клик на update.bat
```

**Когда использовать:**
- Положили новый прайс-лист в `price_files/`
- Нужно обновить данные в дашборде

---

### 3. `watch.bat` - Автоматическое отслеживание
**Что делает:**
- Постоянно следит за изменениями в `price_files/`
- Автоматически обновляет JSON при изменении Excel файла
- Работает в фоновом режиме

**Использование:**
```
Двойной клик на watch.bat
Оставьте окно открытым
```

**Когда использовать:**
- Вы часто обновляете прайс-листы
- Нужно автоматическое обновление без ручного запуска

**Остановка:** Ctrl+C в окне командной строки

---

### 4. `update-silent.bat` - Тихое обновление
**Что делает:**
- То же что `update.bat`, но без вывода сообщений
- Используется для автоматизации

**Использование:**
Для вызова из других скриптов или планировщика задач

---

## 🚀 Быстрый старт

### Первый запуск:
1. Убедитесь, что Node.js установлен
   - Если нет: скачайте с https://nodejs.org/
2. **Двойной клик на `start.bat`**
3. Дашборд откроется автоматически!

### Обновление прайс-листа:
1. Скопируйте новый Excel файл в папку `price_files/`
2. **Двойной клик на `update.bat`**
3. Обновите страницу в браузере (F5)

---

## 📋 Требования

- **Windows 7/8/10/11**
- **Node.js** 14+ (https://nodejs.org/)
- **Excel файлы** в формате `.xlsx` в папке `price_files/`

---

## ❓ Решение проблем

### "CMD не поддерживает пути UNC в качестве текущей папки"
**Проблема:** Проект находится на сетевом диске (\\server\share\...)

**Решение автоматическое (версия 2.0+):**
- Скрипты автоматически определяют UNC путь
- Временно мапят его на букву диска (Z:, Y:, X:, etc.)
- После завершения очищают временный диск
- Просто запустите скрипт - всё работает!

**Если автоматическое решение не работает:**
1. Скопируйте папку проекта на локальный диск (C:\, D:\, E:\)
2. Запустите скрипт оттуда

### "Node.js не установлен"
1. Скачайте Node.js: https://nodejs.org/
2. Установите (все настройки по умолчанию)
3. Перезапустите `start.bat`

### "Нет Excel файлов в price_files"
1. Убедитесь, что файлы имеют расширение `.xlsx`
2. Проверьте, что они находятся именно в папке `price_files/`

### Данные не обновились в браузере
1. Нажмите **Ctrl+Shift+R** (жесткое обновление)
2. Или закройте и откройте вкладку заново

### "EPERM: operation not permitted" при npm install
**Причина:** npm пытается создать файлы в C:\Windows (ошибка с UNC путями)

**Решение:** Используйте обновлённые скрипты (версия 2.0+) с автоматической поддержкой UNC путей

---

## 💡 Советы

### Создание ярлыка на рабочий стол:
1. ПКМ на `start.bat` → Отправить → Рабочий стол (создать ярлык)
2. Переименуйте ярлык: "Склад Эльвира-Кореянка"
3. Теперь можно запускать одним кликом!

### Автозапуск при старте Windows:
1. Win+R → `shell:startup`
2. Скопируйте туда ярлык на `watch.bat`
3. Теперь отслеживание будет запускаться автоматически!

### Планировщик задач (авто-обновление по расписанию):
1. Win → Планировщик заданий
2. Создать задачу
3. Действие: запуск `update-silent.bat`
4. Триггер: ежедневно в 9:00 (или по вашему расписанию)

---

## 🔍 Что происходит внутри

```
start.bat
  ├─ Проверяет Node.js
  ├─ Устанавливает зависимости (npm install)
  ├─ Запускает convert-to-json.js
  │   ├─ Сканирует price_files/
  │   ├─ Находит последний .xlsx по дате изменения
  │   ├─ Читает Excel (SheetJS)
  │   ├─ Парсит данные
  │   └─ Сохраняет в data.json
  └─ Открывает index.html в браузере
```

---

## 📊 Пример использования

### Ежедневная работа:
```
1. Утро:
   - Двойной клик на start.bat
   - Дашборд открывается автоматически

2. Приходит новый прайс:
   - Сохраняете Excel в price_files/
   - Двойной клик на update.bat
   - F5 в браузере

3. Конец дня:
   - Просто закрываете браузер
```

### Для продвинутых:
```
1. Запускаете watch.bat один раз
2. Оставляете окно открытым
3. Просто обновляете Excel в price_files/
4. JSON обновляется автоматически
5. Нажимаете F5 в браузере
```

---

📖 **Полная документация**: см. README.md
