# 🪟 Запуск из Windows (WSL)

## Проблема

Проект находится в WSL по пути:
```
\\wsl.localhost\Ubuntu-24.04\home\damien\projects\predzakaz_dashboard
```

Обычные bat-файлы (update.bat, start.bat) не работают с UNC путями WSL.

---

## ✅ Решение: Специальные WSL-скрипты

Я создал специальные bat-файлы для запуска из Windows:

### 1. **start-wsl.bat** - Быстрый запуск (РЕКОМЕНДУЕТСЯ)
```cmd
Двойной клик на start-wsl.bat
```

**Что делает:**
- ✅ Запускает команды в WSL окружении
- ✅ Проверяет Node.js в WSL
- ✅ Устанавливает зависимости (при первом запуске)
- ✅ Конвертирует последний Excel в JSON
- ✅ **Автоматически открывает дашборд в браузере Windows**

---

### 2. **update-wsl.bat** - Обновление данных
```cmd
Двойной клик на update-wsl.bat
```

**Когда использовать:**
- Положили новый Excel в `price_files/`
- Нужно обновить данные без перезапуска

---

### 3. **watch-wsl.bat** - Автоматическое отслеживание
```cmd
Двойной клик на watch-wsl.bat
```

**Что делает:**
- Постоянно следит за изменениями Excel файлов
- Автоматически обновляет JSON при изменениях
- Оставьте окно открытым для непрерывной работы

**Остановка:** Ctrl+C в окне

---

## 🚀 Быстрый старт для WSL

### Первый запуск:

1. **Откройте проводник Windows**
2. **Перейдите по адресу:**
   ```
   \\wsl.localhost\Ubuntu-24.04\home\damien\projects\predzakaz_dashboard
   ```

3. **Двойной клик на `start-wsl.bat`**

4. **Дашборд откроется автоматически!**

### Обновление прайс-листа:

1. Скопируйте новый Excel в папку `price_files/`
2. Двойной клик на `update-wsl.bat`
3. Обновите страницу в браузере (F5)

---

## 📋 Требования

### В Windows:
- ✅ Windows 10/11 с WSL 2
- ✅ WSL установлен и запущен

### В WSL (Ubuntu):
- ✅ Node.js 14+ установлен

**Проверка WSL:**
```cmd
wsl --list
```

**Установка Node.js в WSL (если нужно):**
```bash
# В Windows CMD или PowerShell:
wsl bash -c "curl -fsSL https://deb.nodesource.com/setup_lts.x | sudo -E bash -"
wsl bash -c "sudo apt-get install -y nodejs"
```

---

## ❓ Решение проблем

### "WSL is not installed or not running"

**Решение:**
```cmd
# Проверьте статус WSL:
wsl --list --verbose

# Если не установлен:
wsl --install

# Если остановлен, запустите Ubuntu:
wsl
```

### "Node.js is not installed in WSL"

**Решение:**
```cmd
# Установите Node.js в WSL:
wsl bash -c "curl -fsSL https://deb.nodesource.com/setup_lts.x | sudo -E bash -"
wsl bash -c "sudo apt-get install -y nodejs"

# Проверьте установку:
wsl node --version
```

### Браузер не открывается автоматически

**Решение:**
Откройте вручную в браузере:
```
\\wsl.localhost\Ubuntu-24.04\home\damien\projects\predzakaz_dashboard\index.html
```

### Медленная работа через WSL

**Альтернатива 1: Скопировать на диск Windows**
```cmd
# Скопируйте проект в Windows:
xcopy "\\wsl.localhost\Ubuntu-24.04\home\damien\projects\predzakaz_dashboard" "C:\Projects\predzakaz_dashboard" /E /I

# Используйте обычные bat-файлы:
cd C:\Projects\predzakaz_dashboard
start.bat
```

**Альтернатива 2: Работать полностью в WSL**
```bash
# В WSL терминале:
cd /home/damien/projects/predzakaz_dashboard
bash update.sh
xdg-open index.html  # Откроет в браузере Linux (если установлен)
```

---

## 💡 Создание ярлыка на рабочий стол

1. **Откройте проводник:**
   ```
   \\wsl.localhost\Ubuntu-24.04\home\damien\projects\predzakaz_dashboard
   ```

2. **ПКМ на `start-wsl.bat`** → **Отправить** → **Рабочий стол (создать ярлык)**

3. **Переименуйте ярлык:** "Склад Эльвира-Кореянка (WSL)"

4. **Готово!** Теперь запуск в один клик с рабочего стола

---

## 🔄 Сравнение: Обычные vs WSL bat-файлы

| Файл | Где работает | Для кого |
|------|--------------|----------|
| `update.bat` | Локальный диск (C:\, D:\) | Проект на Windows |
| `start.bat` | Локальный диск (C:\, D:\) | Проект на Windows |
| `watch.bat` | Локальный диск (C:\, D:\) | Проект на Windows |
| **`update-wsl.bat`** | WSL (\\wsl.localhost\...) | **Проект в WSL** |
| **`start-wsl.bat`** | WSL (\\wsl.localhost\...) | **Проект в WSL** |
| **`watch-wsl.bat`** | WSL (\\wsl.localhost\...) | **Проект в WSL** |

**Ваш случай:** Используйте **WSL-версии** (`*-wsl.bat`)

---

## 📊 Структура файлов проекта

```
predzakaz_dashboard/
├── 🪟 Для Windows (локальный диск):
│   ├── start.bat           # Быстрый запуск
│   ├── update.bat          # Обновление данных
│   └── watch.bat           # Авто-отслеживание
│
├── 🐧 Для WSL:
│   ├── start-wsl.bat       # Быстрый запуск из Windows → WSL
│   ├── update-wsl.bat      # Обновление данных из Windows → WSL
│   ├── watch-wsl.bat       # Авто-отслеживание из Windows → WSL
│   ├── start.sh            # Запуск в Linux терминале
│   └── update.sh           # Обновление в Linux терминале
│
├── 📊 Основные файлы:
│   ├── index.html          # Веб-дашборд
│   ├── convert-to-json.js  # Конвертер Excel → JSON
│   ├── data.json           # Данные (генерируется автоматически)
│   └── package.json        # Зависимости Node.js
│
└── 📁 price_files/         # Сюда кладите Excel файлы
    └── *.xlsx
```

---

## ✅ Всё готово!

**Теперь просто запускайте `start-wsl.bat` и работайте! 🚀**

Если есть вопросы, см. [README.md](README.md) или [BAT-FILES-README.md](BAT-FILES-README.md)
