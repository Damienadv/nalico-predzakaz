# БЫСТРАЯ ИНСТРУКЦИЯ 🚀

## 🪟 Для пользователей Windows

### Проект на локальном диске (C:\, D:\, E:\):
1. **Двойной клик** на `start.bat`
2. Дашборд откроется автоматически!

### Проект в WSL (\\wsl.localhost\...):
1. **Двойной клик** на `start-wsl.bat`
2. Дашборд откроется автоматически!

📖 **Подробная инструкция для WSL:** [WSL-README.md](WSL-README.md)

📖 **Подробная инструкция для bat-файлов:** [BAT-FILES-README.md](BAT-FILES-README.md)

---

## 🐧 Для пользователей Linux/Mac

### Первый запуск

1. **Установите зависимости** (один раз):
   ```bash
   npm install
   ```

2. **Конвертируйте Excel в JSON**:
   ```bash
   node convert-to-json.js
   ```
   Или просто:
   ```bash
   npm run convert
   ```

3. **Откройте** `index.html` в браузере

## Обновление прайс-листа

Когда получаете новый Excel файл:

1. **Скопируйте** новый файл в папку `price_files/`
2. **Запустите скрипт обновления**:
   ```bash
   ./update.sh
   ```
   Или:
   ```bash
   npm run update
   ```
3. **Обновите** страницу в браузере (F5)

## Проблемы?

### "Не удалось загрузить data.json"
```bash
node convert-to-json.js
```

### Данные не обновились
1. Проверьте, что новый файл в `price_files/`
2. Запустите: `node convert-to-json.js`
3. Обновите страницу: F5 (или Ctrl+Shift+R для жесткого обновления)

## Варианты запуска

**Без сервера** (простой способ):
- Просто откройте `index.html` в браузере
- Данные загружаются из `data.json`

**С сервером** (для продвинутых):
```bash
npm start
```
Откройте: http://localhost:3000

---

📄 Подробная инструкция: см. README.md
