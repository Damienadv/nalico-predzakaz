# Склад "Эльвира-Кореянка" 📦

Интерактивный дашборд для просмотра и управления прайс-листами товаров.

## 🚀 Быстрый старт

### Вариант 1: Без сервера (Рекомендуется)

1. **Конвертируйте Excel в JSON** (при первом запуске или обновлении прайс-листа):
   ```bash
   node convert-to-json.js
   ```
   Или:
   ```bash
   npm run convert
   ```

2. **Откройте файл** `index.html` в любом браузере
   - Данные загрузятся автоматически из `data.json`
   - Работает полностью оффлайн
   - Не требует запуска сервера

### Вариант 2: С сервером (для продвинутых функций)

1. **Запустите сервер**:
   ```bash
   npm start
   ```

2. **Откройте в браузере**: http://localhost:3000

## 📋 Обновление данных

### Когда у вас новый прайс-лист:

1. **Положите новый Excel файл** в папку `price_files/`
2. **Запустите конвертацию**:
   ```bash
   node convert-to-json.js
   ```
   Скрипт автоматически найдет последний файл по дате изменения
3. **Обновите страницу** в браузере (F5)

### Автоматизация обновления:

Можно настроить автоматическое обновление при изменении файлов в `price_files/`:

**Windows (PowerShell)**:
```powershell
# Создайте файл watch.ps1
while($true) {
    Get-ChildItem -Path "price_files" -Filter "*.xlsx" | 
    ForEach-Object {
        node convert-to-json.js
    }
    Start-Sleep -Seconds 60
}
```

**Linux/Mac**:
```bash
# Используйте watch или inotify
watch -n 60 'node convert-to-json.js'
```

## 🎯 Функции

✅ **Автоматическая загрузка** - данные загружаются из JSON при открытии  
✅ **Без сервера** - работает как обычный HTML файл  
✅ **Быстро** - JSON загружается мгновенно  
✅ **Фильтрация** - по бренду, наличию, поиску  
✅ **Сортировка** - по любому столбцу  
✅ **Темная тема** - переключение светлой/темной темы  
✅ **Экспорт** - сохранение отфильтрованных данных в CSV  
✅ **Статистика** - товары в наличии, последнее обновление  
✅ **Responsive** - работает на мобильных устройствах  

## 📁 Структура проекта

```
predzakaz_dashboard/
├── index.html              # Основной дашборд
├── data.json               # Данные в JSON формате (автогенерируется)
├── convert-to-json.js      # Скрипт конвертации Excel → JSON
├── server.js               # HTTP сервер (опционально)
├── package.json            # Зависимости Node.js
├── price_files/            # Директория с Excel файлами
│   └── ПРАЙС на 8 января.xlsx
└── README.md              # Эта инструкция
```

## 🔧 Требования

- **Node.js** 14+ (только для конвертации Excel → JSON)
- **Современный браузер** (Chrome, Firefox, Safari, Edge)

## 📝 Формат Excel файла

Дашборд ожидает следующие столбцы:
- Бренд
- Наименование
- Категория/Описание (опционально)
- Артикул (опционально)
- Объем, мл
- РРЦ
- Стоимость, руб
- Заказ, шт
- Итого
- Остаток склад

## 🆘 Решение проблем

### Данные не загружаются
1. Убедитесь, что файл `data.json` существует
2. Запустите: `node convert-to-json.js`
3. Проверьте консоль браузера (F12) на ошибки

### Ошибка "Cannot find module 'xlsx'"
```bash
npm install
```

### Нужно загрузить Excel файл вручную
Нажмите кнопку "📁 Загрузить файл" и выберите Excel файл напрямую

## 💡 Преимущества JSON подхода

1. **Быстрая загрузка** - JSON загружается в 10 раз быстрее Excel
2. **Не нужен сервер** - работает локально
3. **Кеширование** - браузер кеширует JSON
4. **Простота** - просто откройте HTML файл
5. **Оффлайн** - работает без интернета

## 🔄 Изменения (версия 2.0)

- ✅ Переименован на "Склад Эльвира-Кореянка"
- ✅ Убрана карточка "Общая стоимость остатков"
- ✅ Автоматическая загрузка из JSON файла
- ✅ Не требует сервер для базовой работы
- ✅ Скрипт автоматической конвертации Excel → JSON

## 📞 Поддержка

При возникновении проблем:
1. Проверьте консоль браузера (F12)
2. Убедитесь, что `data.json` существует и содержит данные
3. Перезапустите конвертацию: `node convert-to-json.js`
